from django.forms import Form, CharField, EmailField, SlugField, ValidationError, ModelChoiceField, IntegerField, ModelForm
from django.forms.widgets import HiddenInput

class PreferenceSubmitForm(Form):
    # override in init to restrict choices
    # should be on contracted days for child
    # maybe also relative to classroom
    shift_1 = ModelChoiceField(queryset=Shift.objects.filter(classroom=None))
 
    def __init__(self, *args, **kwargs):
        self.kwargs.pop('classroom')
        super().__init__(*args, **kwargs)
        self.fields['']
