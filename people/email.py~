from django.conf import settings
from django.core.mail import send_mail
from django.contrib.templates import render_to_string


def send_invite_email('email', 'kid_shortname'):
    subject = 'bigelow worktime invitation'
    message=render_to_string('people/email/invite.txt',
                             {'kid_shortname':kid_shortname,
                              'email':email, 'token':token}),
    email_from = settings.EMAIL_HOST_USER
    recipient_list = [email]
    send_mail(subject, message, email_from, recipient_list)


