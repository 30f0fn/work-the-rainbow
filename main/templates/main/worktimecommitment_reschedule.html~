{% extends "worktime/base.html" %}

{% load people_tags %} 
{% load rules %}

{% block content %}

  <h1>
    upcoming
  </h1>
  {% if user.children %}
    <h2>
      worktime shifts
    </h2>
    {% if view.worktime_commitments %}
      <ul>
        {% for worktime_commitment in view.worktime_commitments %}
          <li>
            {{worktime_commitment.date | date:"F d"}}, 
            {{worktime_commitment.start_time | time:"H:i"}}
            ({{worktime_commitment.family.classroom.slug}})
            <a href="{% url 'reschedule-worktimecommitment' worktime_commitment.pk %}">
              reschedule
            </a>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      none soon
    {% endif %}
  {% endif %}

  <h2>
    events
  </h2>
  {% if view.events %}
    <ul>
      {% for event in view.events %}
        <li>
          {{event.date}}
          {% if event.start_time %}, {{ event.start_time }} {% endif %}
          {% if event.end_time %} - {{ event.end_time }} {% endif %}
        </li>
      {% endfor %}
    </ul>
  {% else %}
    none soon 
  {% endif %}


  <h2>
    holidays
  </h2>
  {% if view.holidays %}
    <ul>
      {% for holiday in view.holidays %}
        <li>
          {{holiday.date}}
          {% if holiday.end_date %} - {{ event.end_date }} {% endif %}
        </li>
      {% endfor %}
    </ul>
  {% else %}
    none soon 
  {% endif %}

{% endblock content %}
